interface:
  display_name: "Develop Loop"
  short_description: "PR fixes with approval gates."
  default_prompt: "Use $develop-loop to work through these PR comments one issue at a time. Do not commit, push, or resolve review threads until I approve the current fix."
