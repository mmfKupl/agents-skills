interface:
  display_name: "Develop Task"
  short_description: "Implement with reviewed gates, validation, commits, and a draft PR."
  default_prompt: "Use $develop-task to implement this repository task with OpenSpec when warranted, preflight/postflight review gates, focused validation, logical commits, and a draft PR. If this is analysis-only, do not edit files or run side-effecting actions."
