interface:
  display_name: "Develop Task"
  short_description: "Route reviewed implementation through one writer."
  default_prompt: "Use $develop-task to implement this repository change with mandatory review gates, adaptive model routing, one writer at a time, focused validation, and standalone lifecycle handling when applicable."
policy:
  allow_implicit_invocation: false
