interface:
  display_name: "Develop Task"
  short_description: "Run reviewed work through supervised agents."
  default_prompt: "Use $develop-task to implement this repository change with runner-supervised fresh-context review and implementation jobs; use direct subagents only when I explicitly opt out of the runner."
policy:
  allow_implicit_invocation: false
