interface:
  display_name: "Review Task"
  short_description: "Deep audit of implemented tasks."
  default_prompt: "Use $review-task to deeply review this implemented repository task, inspect the diff and requirements, use read-only specialist delegation when useful, satisfy stated validation bars, reuse already-passed checks when valid, run focused validation when useful, and report all credible risks."
