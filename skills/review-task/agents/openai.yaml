interface:
  display_name: "Review Task"
  short_description: "Deep indexed audit of implemented tasks"
  default_prompt: "Use $review-task to deeply audit this implemented repository task, assign stable issue-local review and finding identities, verify lifecycle against current code, and return the complete read-only review protocol."

policy:
  allow_implicit_invocation: false
